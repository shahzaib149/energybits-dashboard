/**
 * SEO Analytics — configured when AIRTABLE_API_KEY is set.
 * Base and table IDs are resolved via the Airtable Meta API.
 */

export { isAirtableConfigured as isSEOAnalyticsConfigured } from "@/lib/airtable/config/env";
